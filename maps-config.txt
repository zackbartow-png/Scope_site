# Koehn Scope Builder Google Maps configuration
# Paste the raw keys after the equals sign. DO NOT add quotes.
# Keep each key on one line.
GOOGLE_MAPS_JAVASCRIPT_API_KEY=
GOOGLE_MAPS_STATIC_API_KEY=
# Optional: leave blank to reuse the Static Maps key. Enable Street View Static API on that key/project.
GOOGLE_STREET_VIEW_STATIC_API_KEY=
